<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Tracker</title>
    <style>
        body{
            background-color: rgba(100, 148, 237, 0.671);
        }
        h1{
            text-align: center;
        }
        input{
            width: 300px;
            height: 30px;
            margin-top: 50px;
            margin-left: 520px;
        }
        input:nth-of-type(3){
            font-size: larger;
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <h1><%=title%></h1>
    <form action="/login" method="post">
        <input type="text" name="tracker" placeholder="Enter the expense"><br>
        <input type="text" name="values" placeholder="Enter the Amount"><br>
        <input type="submit">
    </form>
    <% total.forEach(function (item){ %>
      <li>Expense: <%= item.expense%>| Values: <%= item.value%>  </li>
    <% } ) %>
    <br>

    <form action="/remove" method="post">
        <button type="submit" name="Removess" onclick="remove()">Remove The First Expense</button>
    </form>
</body>
</html>